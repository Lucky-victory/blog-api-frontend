import { AuthorDirective } from './author.directive';

describe('AuthorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorDirective();
    expect(directive).toBeTruthy();
  });
});
